collections_config:
  site_data:
    path: src/data

collection_groups:
  pages:
    path: src/pages
    type: astro

data_config:
  collections:
    - pages

source:

_inputs:
  src:
    type: image

paths:
  static: ""
  uploads: uploads

source_editor:

timezone: Etc/UTC
